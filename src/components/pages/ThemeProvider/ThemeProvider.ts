import { createTheme } from "@mui/material";

const theme =  createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#9569d1",
    },
    secondary: {
      main: "#dcc2ff",
    },
    background: {
    },
  },
});
export default theme;
