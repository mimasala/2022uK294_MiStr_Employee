import { createTheme } from "@mui/material";

const theme =  createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#dcc2ff",
    },
    secondary: {
      main: "#dcc2ff",
    },
    background: {
      default: "#36373d",
    },
  },
});
export default theme;